# Constants (User configurable)

APP_NAME = 'AndroidCheckerWorker'  # Used to generate derivative names unique to the application.

AWS_REGION = ''
AWS_PROFILE = 'default'  # The same profile used by your AWS CLI installation

EC2_INSTANCE_ID = ''
